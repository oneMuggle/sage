# feat(right-panel): R5+R6——文件修改审查体验（聊天流 diff 卡片 + 变更实时刷新 + 预览/词级高亮/汇总审查）

对标 Cursor / Copilot Edits / Cline / GitHub Review 的文件修改审查体验，四个批次。

## 批次 R5 聊天流文件修改卡片 + 变更实时刷新
- **聊天流 inline diff 卡片**（新 `FileChangeCard`）：`write_file` / `edit_file` / `apply_patch` 的工具卡片下方展示 文件路径 + `+N/−M` 行数徽章，展开就地懒加载该文件工作区 diff（Shiki 高亮）；apply_patch 多文件逐文件各一张卡；面板按钮经 `rightPanelStore.selectChange`（selectArtifact 同款模式）直达右侧变更 Tab 对应文件 diff 视图
- **事件驱动刷新**：新增 `backend/data/workspace_events.py` 监听器（镜像 artifact_repo S7 模式），三个写文件工具成功落盘后广播 `workspace_changed` → legacy_routes 推送活跃流 → 前端 `changesListStore.fetchDebounced`（800ms 尾沿防抖），变更徽标实时化，不再依赖手动刷新
- **numstat 行数统计**：`/changes` 接口新增 `insertions/deletions`（`git diff HEAD --numstat`，未跟踪文件退化行数统计，二进制为 None）；变更列表行渲染 +/- 徽章
- **未跟踪文件 diff**：`/changes/diff` 对未跟踪文件回退生成 `new file` 全加号 diff，替换原"未跟踪文件:暂无 diff 内容"空态

## 批次 R6·A/B 预览 / 词级高亮 / 消息汇总条
- **预览视图**：后端新增 `GET /changes/file` 工作区文件内容只读（realpath 边界守卫同 write_file 口径 + 2MiB 读取上限 + NUL 二进制嗅探 415 + 512KiB 截断）；ChangesSection diff 视图头部 Diff/预览 分段切换，预览按扩展名选 Shiki 语言渲染修改后全文
- **SplitDiff 词级行内高亮**：modify 行复用 `textDiff.diffSpans` 字符级切分，行内真正变化片段染深色（GitHub/VSCode 观感）；超 300 字符退化整行染色（与 office 预览同口径）
- **消息级修改汇总条**：`FileChangeCards` 组组件——一次工具调用 <3 个文件平铺，≥3 个默认折叠为"修改了 N 个文件 +X/−Y"汇总 chip
- **EXT_LANG 上抬**：扩展名→Shiki 语言映射从 ArtifactViewer 抽为 `shared/lib/fileLang`，产物预览与变更预览共用

## 批次 R6·C 全部审查汇总视图（P2-7）
- 变更 Tab 新增"全部审查"入口：一次拉取全仓库 diff（复用 `GET /changes/diff` 空 path 口径），按 `diff --git` 切分为逐文件小节连续滚动
- 顶部文件 chip 锚点导航（scrollIntoView）；每节文件头含 +/- 合计与折叠开关；截断显示 64KiB 提示横幅；二进制文件占位
- 切分纯函数独立于 `reviewSections.ts`（删除文件回落旧侧路径等边界有单测）；撤销仍回列表逐文件/逐 hunk 操作

## 批次 R6·D 大 diff 性能（P1-6）
- **SplitDiff 渐进渲染**：首屏只渲染前 400 行，滚动接近底部经 IntersectionObserver 哨兵自动追加（无 IO 环境退化为"加载更多"按钮）；词级 diffSpans 只算可见行，未挂载行不付 O(n·m) 成本
- **unified 视图逐 hunk 折叠**：每 hunk 头部折叠开关，折叠态跳过 Shiki 渲染、勾选框保留（可勾选撤销）；超过 8 个 hunk 时后续默认折叠

## 已知取舍
- `workspace_changed` 事件 path 为工具视角路径，前端刷新语义以 git status 全量为准（事件只承担"工作区脏了"信号）
- 全 diff 虚拟滚动（@tanstack/react-virtual）未采用：渐进渲染 + ShikiCodeBlock 内置 >30 行折叠已覆盖现有体量，如未来出现万行级单文件 diff 再评估

## 测试
- 后端：workspace_changed 事件链路单测 8（含三工具钩子/异常隔离/失败不广播）、变更路由集成 +11（numstat/二进制 None/new-file diff/未变更不误触/预览内容/越界 403/缺失 404/二进制 415/截断）
- 前端：新增 FileChangeCard 8、SplitDiff 6（词级 4 + 渐进 2）、ReviewAll + reviewSections 8、ChangesSection +5（徽章/selectChange 直达/预览切换/全部审查/逐 hunk 折叠）、orchestrationEvents +1（防抖合并）
- 全量 175 文件 / 1368 用例全绿；tsc、electron tsc、eslint、ruff 全绿；CI All Checks pass（Backend Python 3.11/3.8、Frontend、Electron build ubuntu/windows、playwright smoke/deep/live-boot）

🤖 Generated with [ZCode](https://zcode.ai)
