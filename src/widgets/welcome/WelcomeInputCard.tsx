// TODO(M6): Welcome 屏占位 stub,M6 实施时替换为正式实现。
// 仅为满足被 skip 的 M6 测试文件的 import resolve,无功能。
// 接受 main M6 组件的所有可能 props(placeholder / onSend / typewriterPhrases)。
export function WelcomeInputCard(_props: {
  placeholder?: string;
  onSend?: (msg: string) => void;
  typewriterPhrases?: string[];
}): null {
  return null;
}
