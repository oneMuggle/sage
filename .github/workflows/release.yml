name: Release Build

# Phase 0 (2026-06-13) 占位:原 Tauri Windows NSIS 构建 job 已删除
# (工作目录 src-tauri/ 已归档至 archive/src-tauri-2026-06-13-win7-migration/)
# 替代方案: Electron 21 + electron-builder NSIS pipeline,见
# docs/technical/XX-electron-win7.md (Phase 6 出)
# 当前 workflow 保留 trigger 配置,Phase 1 实现 build + release job 后激活
on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (如 0.1.1)'
        required: true
        default: '0.1.1'

jobs:
  placeholder:
    name: Phase 1 placeholder (electron-builder)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO Phase 1: electron-builder NSIS pipeline + GH release"