"""Regression test for issue #999 (example).

This is a placeholder regression test demonstrating the naming convention.
Replace with actual regression tests tied to real GitHub issues.

Naming: regressions/<issue-number>-<short-slug>.test.ts
From pi's testing strategy.
"""

import pytest


def test_example_regression_placeholder():
    """Example regression test placeholder.

    Replace this with actual regression tests tied to real issues.
    Each regression test should:
    1. Reproduce the bug (fail with old code)
    2. Pass after the fix
    3. Be named with the issue number
    """
    # This is a placeholder - remove when adding real regression tests
    assert True


if __name__ == '__main__':
    pytest.main([__file__, '-v'])
