"""Evolution stub routes (placeholder — implemented in Task 6).

Registers zero routes in this initial scaffold. Task 6 will populate this
function with the evolution endpoints (3 routes per the plan).

Contract:
    register_evolution_routes(registry: dict) -> None

    ``registry`` is a dict populated by chat.py / orchestration.py / etc.
    Keys are ``(method: str, path_regex: str)`` tuples, values are callables
    with signature ``fn(ctx: StubContext, body: dict, **path_groups) -> None``.
"""
from __future__ import annotations


def register_evolution_routes(registry: dict) -> None:
    """Evolution routes — implemented in Task 6."""
    pass
