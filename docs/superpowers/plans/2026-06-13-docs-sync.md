# Plan D: 文档同步（win7 BRANCH_NOTES + main docs 重写）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 同步两份文档状态：
1. **win7 分支** `BRANCH_NOTES.md`：把过时的"Tauri 1.6 + Python 3.8"内容更新为"Electron 21.4.4 + Python 3.8（最新迁移）"
2. **main 分支** `docs/technical/20-electron-win7.md` → `20-electron.md`（去掉 Win7 限定，作为通用 Electron 文档）

**Architecture:** 这是两个独立分支的两份独立 PR：
- PR-1：win7 分支更新 BRANCH_NOTES
- PR-2：main 分支重写 20-electron.md + 新增 21-win7-lts.md + 归档 20-win7-tauri-compat.md

**Tech Stack:** Markdown, Git, GitHub PR

**前提事实**：

- win7 当前 `BRANCH_NOTES.md` 仍写"Tauri 1.6 + Python 3.8"，与实际代码（Electron 21.4.4 + Python 3.10）不一致
- main 当前 `docs/technical/20-electron-win7.md` 是 win7 写的，含 Win7 特定内容
- main 当前没有 `21-win7-lts.md`
- main 当前没有 `20-win7-tauri-compat.md`（这文件可能在 archive/ 或已删）
- main 当前 `docs/technical/README.md` line 16 引用 `20-electron-win7.md`

---

## File Structure

| File | Action | Branch | Responsibility |
|---|---|---|---|
| `release/win7/BRANCH_NOTES.md` | Modify | release/win7 | 把 "Tauri 1.6 + Python 3.8" → "Electron 21.4.4 + Python 3.8" |
| `docs/technical/20-electron-win7.md` | Rename | main | 改名为 `20-electron.md` |
| `docs/technical/20-electron.md` | Create (from rename) | main | 通用 Electron 文档（去掉 Win7 限定） |
| `docs/technical/21-win7-lts.md` | Create | main | 新增：Win7 LTS 独立章节 |
| `docs/technical/20-win7-tauri-compat.md` | Archive | main | 归档（如存在） |
| `docs/technical/README.md` | Modify | main | 更新章节目录（line 16） |
| `docs/11-structure.md` | Modify | main | 加 Electron 章节 |
| `docs/design.md` | Modify | main | 桌面壳段落更新（Plan A 也动，但 Plan D 细化） |
| `archive/tech-docs-2026-06-13/` | Create | main | 归档目录（如果不存在） |

---

## Task 1: 在 win7 分支更新 BRANCH_NOTES.md

**Files:**
- Modify: `release/win7/BRANCH_NOTES.md`

- [ ] **Step 1: 切换到 win7 分支**

```bash
cd /home/fz/project/sage
git switch release/win7
git pull --rebase origin release/win7
```

- [ ] **Step 2: 建 feature 分支**

```bash
git switch -c docs/branch-notes-refresh
```

- [ ] **Step 3: 找 Tauri 1.6 提及位置**

```bash
grep -n "Tauri 1.6\|Python 3.8\|WebView2" BRANCH_NOTES.md
```

Expected: 看到至少 5 行匹配

- [ ] **Step 4: 替换 "Tauri 1.6" → "Electron 21.4.4"**

用 Edit 工具，全文替换：

```diff
- Tauri 1.6 + Python 3.8
+ Electron 21.4.4 + Python 3.8
```

- [ ] **Step 5: 替换 "本分支的特殊性" 段**

找到该段（"## 本分支的特殊性"），整段替换为：

```diff
- ## 本分支的特殊性
-
- 1. **CI 用 GH-hosted `windows-latest` (Server 2022) 做 cross-build**（**interim 模式**——Win7 self-hosted runner 当前不可用）：
-    - `ci-win7.yml` 和 `release-win7.yml` 都用 `runs-on: windows-latest`
-    - cross-build 编译产物配置为 Win7 兼容（tauri=1.6 + windows7-compat + WebView2 v109 fallback）
-    - **不**做 `sage.exe` 启动冒烟（WebView2 v109 fallback 行为只在真 Win7 上触发，Server 2022 上无法验证）
-    - **Win7 启动验证 = 人工步骤**：维护者下载 artifact 拷贝到 Win7 物理机双击运行
-    - 未来：拿到 Win7 物理机/VM 后，把 `runs-on` 改回 `[self-hosted, windows-7]` + 恢复 smoke test
- 2. **WebView2 完全离线安装**：`tauri.conf.json` 设 `webviewInstallMode: "offlineInstaller"`。Tauri 1.6 在 `tauri build` 时从微软官方下载 `MicrosoftEdgeWebView2RuntimeInstallerX64.exe`（~127MB，官方签名）并 embed 到 MSI/NSIS。**Win7 上自动 fallback 到 v109**（最后兼容版本，微软官方机制）。用户完全离线可用，无需任何手动步骤。
- 3. **`tauri = { features = ["windows7-compat"] }`**：使用旧版 WebView2 SDK (webview2-com) 而非新版 (webview2)。
+ ## 本分支的特殊性
+
+ 1. **CI 用 GH-hosted `windows-latest` (Server 2022) 做 cross-build**（**interim 模式**——Win7 self-hosted runner 当前不可用）：
+    - `.github/workflows/ci.yml` 中 `backend-py38` job + `desktop-build` (matrix: ubuntu/windows) + `electron-smoke` job
+    - `desktop-build` cross-build 编译产物配置为 Win7 兼容（electron@21.4.4 + 7 个 Win7 启动开关）
+    - **不**做真 Win7 启动冒烟（Win7 兼容性行为只在真 Win7 上触发，Server 2022 上无法验证）
+    - **Win7 启动验证 = 人工步骤**：维护者下载 artifact 拷贝到 Win7 物理机双击运行
+    - 未来：拿到 Win7 物理机/VM 后，把 `runs-on` 改回 `[self-hosted, windows-7]` + 恢复 smoke test
+ 2. **Chromium 106 内嵌**：Electron 21.4.4 自带 Chromium 106 + Node 16.20.2，**不依赖系统 WebView2**。Win7 上无需安装任何外部 runtime，**即装即用**。
+ 3. **Win7 启动开关**（`electron/main.ts` 在 `app.whenReady()` 之前设置）：
+    - `app.disableHardwareAcceleration()` 禁用 GPU 硬件加速
+    - `--no-sandbox` 禁用 chrome-sandbox（Win7 无 SUID helper）
+    - `--disable-gpu` 强制 CPU 合成路径
+    - `--disable-software-rasterizer` 关闭 Skia 软件光栅
+    - 等等（详见 `docs/technical/20-electron.md` § Win7 Launch Flags）
```

- [ ] **Step 6: 替换 "风险声明" 段**

找到 "⚠️ **本分支基于已 EOL 的技术栈**" 段，整段替换为：

```diff
- ⚠️ **本分支基于已 EOL 的技术栈**——Tauri 1.6 + Python 3.8 + WebView2 v109 + Rust 1.77.2。
- 新发现的 CVE 大概率**不会得到修复**。用户应：
+ ⚠️ **本分支基于已 EOL 的技术栈**——Electron 21.4.4 + Python 3.8 + Chromium 106 + Node 16.20.2。
+ 新发现的 Electron 21 CVE **不会得到官方修复**（Electron 22+ 已 EOL Win7）。用户应：
```

- [ ] **Step 7: 替换 "发布" 段**

找到 "## 发布" 段，替换：

```diff
- 本分支通过 GitHub Actions `release-win7.yml`（独立 workflow，不在 `release.yml` 内）产出预编译安装包：
- - `sage_<version>_x64-setup.exe`（Tauri 1.6 NSIS 安装包，~140MB，含 WebView2 v109 离线安装器）
- - `sage_<version>_x64.msi`（Tauri 1.6 MSI 安装包，~140MB，含 WebView2 v109 离线安装器）
+ 本分支通过 GitHub Actions `release.yml` 触发 electron-builder 产出预编译安装包：
+ - `sage_<version>_x64-setup.exe`（Electron 21.4.4 NSIS 安装包，~110MB，自带 Chromium 106）
+ - `sage_<version>_x64.AppImage`（Linux，仅 main 分支产）
+ - `sage_<version>_x64.dmg`（macOS，仅 main 分支产）
```

- [ ] **Step 8: 验证无遗留 Tauri 1.6 字样**

```bash
grep -n "Tauri 1.6\|tauri 1.6\|tauri=1.6" BRANCH_NOTES.md
```

Expected: 无输出

- [ ] **Step 9: 跑格式检查**

```bash
npx prettier --check BRANCH_NOTES.md
```

Expected: `Checking formatting... All matched files use Prettier code style!`

- [ ] **Step 10: 提交 + 推 + 开 PR**

```bash
git add BRANCH_NOTES.md
git commit -m "docs(win7): refresh BRANCH_NOTES.md — Tauri 1.6 → Electron 21.4.4

- 反映 PR #13 Electron 21.4.4 迁移后的真实技术栈
- 替换 'WebView2 v109' 风险段为 'Chromium 106 + Node 16.20.2'
- 替换 7 个 Win7 启动开关段落（详 20-electron.md）
- 替换发布产物说明（NSIS+MSI → electron-builder 多 OS）

Refs: docs/superpowers/specs/2026-06-13-electron-branch-strategy-design.md"

git push -u origin docs/branch-notes-refresh
gh pr create --target release/win7 --title "docs: refresh BRANCH_NOTES.md (Tauri 1.6 → Electron 21.4.4)" --body "见 commit message"
```

Expected: PR URL 输出（target: release/win7）

---

## Task 2: 在 main 分支重命名 20-electron-win7.md → 20-electron.md

**Files:**
- Rename: `docs/technical/20-electron-win7.md` → `docs/technical/20-electron.md`
- Modify: 内容（去掉 Win7 限定）

> 切到 main 之前先完成 win7 PR（避免冲突）

- [ ] **Step 1: 切到 main**

```bash
cd /home/fz/project/sage
git switch main
git pull --rebase origin main
```

- [ ] **Step 2: 建 feature 分支**

```bash
git switch -c docs/electron-generalization
```

- [ ] **Step 3: 重命名文件**

```bash
git mv docs/technical/20-electron-win7.md docs/technical/20-electron.md
```

- [ ] **Step 4: 修改内容：去掉 Win7 限定**

打开 `docs/technical/20-electron.md`，做以下修改：

**第 1 节标题**：

```diff
- # Win7 + Electron 21 兼容性
+ # Electron 21 桌面壳
```

**第 1 节首段**：

```diff
- > Sage 桌面端的 Win7 兼容栈。Phase 0-3 (2026-06-13) 从 Tauri 2.1.1 迁移到 Electron 21.4.4，Phase 4 接入 CI 矩阵与轻量烟测，Phase 5 留作 release 前真机验证。
+ > Sage 桌面端的统一栈（main + release/win7）。Phase 0-3 (2026-06-13) 从 Tauri 2.1.1 迁移到 Electron 21.4.4，Phase 4 接入 CI 矩阵与轻量烟测，Phase 5 留作 release 前真机验证。Win7 特定内容见 [`21-win7-lts.md`](./21-win7-lts.md)。
```

- [ ] **Step 5: 加新章节"## 5. Win7 LTS 兼容性"**

在文件末尾加：

```markdown
## 5. Win7 LTS 兼容性

> Win7 特定内容（启动开关、BRANCH_NOTES、真机烟测）详见 [`21-win7-lts.md`](./21-win7-lts.md)。
> 本节仅概述 Win7 LTS 兼容性。

### 5.1 Win7 启动开关

`electron/main.ts` 在 `app.whenReady()` 之前设置 7 个 Chromium 开关（见 § 4）。

### 5.2 Win7 LTS 分支

Win7 用户使用 `release/win7` 分支（与 main 共用 Electron 21.4.4，差异在 Python 3.8 钉死 + 18 个月维护窗口）。

详见 [`21-win7-lts.md`](./21-win7-lts.md)。
```

- [ ] **Step 6: 验证文件存在**

```bash
ls -la docs/technical/20-electron.md
wc -l docs/technical/20-electron.md
```

Expected: 文件存在，~350+ 行

---

## Task 3: 新建 21-win7-lts.md

**Files:**
- Create: `docs/technical/21-win7-lts.md`

- [ ] **Step 1: 创建文件**

```bash
cat > docs/technical/21-win7-lts.md <<'EOF'
# Win7 LTS 维护

> Win7 SP1 x64 用户专属章节。`release/win7` 分支 18 个月维护窗口（2026-06-13 → 2027-12-13）。

---

## 1. Win7 LTS 分支定位

| 项 | 值 |
|---|---|
| 分支 | `release/win7` |
| 起点 | PR #13 合并日 (2026-06-13) |
| 终点 | 2027-12-13（分支归档） |
| 桌面壳 | Electron 21.4.4（与 main 一致） |
| Python | 3.8.x（钉死） |
| WebView | 无（Electron 自带 Chromium 106） |
| 入口文档 | [`../technical/20-electron.md`](./20-electron.md) |

## 2. 与 main 的关系

- **main** 持续迭代，Electron 21.4.4 锁定 + Python 3.10+ 演进
- **release/win7** 只接受：
  - 安全修复（Electron 21 已知 CVE 修复不来自官方，由项目评估决定）
  - Win7 特定 bug 修复
  - Python 3.8 兼容性微调
- **不接受**：
  - 新功能
  - 依赖大版本升级（Electron 22+, Python 3.9+）
  - 性能重构
  - a11y 改进

**同步方式**：单 commit cherry-pick，commit message 加 `(cherry picked from main commit XXX)`。

## 3. Win7 启动验证（人工步骤）

> Win7 self-hosted runner 当前不可用，**Win7 启动验证 = 人工步骤**。

1. CI 跑通 `ci.yml`（backend-py38 + desktop-build + electron-smoke 都 success）
2. 维护者下载 artifact `electron-${{ matrix.os }}`（含 .msi / .exe / .AppImage）
3. 拷贝到 **Windows 7 SP1 x64 物理机或 VM**
4. 双击 `.exe`（NSIS 安装包），验证：
   - Electron 21.4.4 启动
   - sage 主窗口出现
   - 基本聊天功能可用
   - Wiki 标签页可打开
5. 在 release/win7 分支的 issue/PR 中报告验证结果

未来拿到 Win7 物理机/VM 后，把 `runs-on: windows-latest` 改回 `runs-on: [self-hosted, windows-7]` 即可恢复完整自动化。

## 4. 18 个月归档时间表

| 阶段 | 时间窗 | 目标 |
|---|---|---|
| **Phase 1：共存期** | 2026-06 ~ 2026-12 | main + win7 并行开发 |
| **Phase 2：减维护期** | 2027-01 ~ 2027-06 | Win7 用户 < 10%，减 patch 频率 |
| **Phase 3：弃用通知期** | 2027-07 ~ 2027-09 | 发"Win7 桌面端 EOL"公告 |
| **Phase 4：归档期** | 2027-10 ~ 2027-12 | 分支 read-only，Release 标 "DEPRECATED" |
| **Phase 5：删除期** | 2027-12-13+ | `git push origin --delete release/win7` |

## 5. Win7 用户迁移到 Web 通道

Phase 3 时：

- 发公告：「Sage 桌面端 Win7 支持将于 2027-12-31 终止」
- 引导用户迁移到 **Chrome 109+ / Firefox ESR 102+** 访问 `https://sage.example.com`
- Chrome 109（2023-03 发布）是**最后支持 Win7** 的版本
- 部署 Web 服务（Sage 后端 + 前端静态资源到任意 HTTPS 主机）

## 6. 风险声明

⚠️ **本分支基于已 EOL 的技术栈**——Electron 21.4.4 + Python 3.8 + Chromium 106 + Node 16.20.2。
新发现的 Electron 21 CVE **不会得到官方修复**（Electron 22+ 已 EOL Win7）。用户应：
- 在隔离网络/虚拟机中使用
- 定期备份数据
- 不要用于处理敏感信息

## 7. Win7 真机烟测脚本

> 真机烟测由 `scripts/win7-smoke/` PowerShell 脚本驱动，详见 [`../../scripts/win7-smoke/README.md`](../../scripts/win7-smoke/README.md)。

- `deploy.ps1` — 部署到 Win7 物理机
- `install.ps1` — 安装 sage
- `launch-test.ps1` — 启动 + 验证
- `verify-ollama.ps1` — 验证 Ollama API
- `teardown.ps1` — 清理
EOF
```

- [ ] **Step 2: 验证文件**

```bash
wc -l docs/technical/21-win7-lts.md
```

Expected: ~80+ 行

---

## Task 4: 归档 20-win7-tauri-compat.md（如存在）

**Files:**
- Move: `docs/technical/20-win7-tauri-compat.md` → `archive/tech-docs-2026-06-13/20-win7-tauri-compat.md`（如存在）

- [ ] **Step 1: 检查文件是否存在**

```bash
ls docs/technical/20-win7-tauri-compat.md 2>/dev/null || echo "NOT FOUND (skip)"
```

- [ ] **Step 2: 如存在，归档**

```bash
mkdir -p archive/tech-docs-2026-06-13
git mv docs/technical/20-win7-tauri-compat.md archive/tech-docs-2026-06-13/
```

- [ ] **Step 3: 验证**

```bash
ls archive/tech-docs-2026-06-13/ 2>/dev/null
```

Expected: 看到 `20-win7-tauri-compat.md`（如果原本存在）

---

## Task 5: 更新 docs/technical/README.md

**Files:**
- Modify: `docs/technical/README.md`

- [ ] **Step 1: 改 line 16 的链接**

找到 line 16：

```diff
- | 20   | [Win7 + Electron 21 兼容性](./20-electron-win7.md) | Tauri → Electron 21 迁移理由 / 7 个 Win7 启动开关 / CI 流水线 / Phase 5 真机烟测 SOP           |
+ | 20   | [Electron 21 桌面壳](./20-electron.md)          | Tauri → Electron 21 迁移理由 / 7 个 Win7 启动开关 / CI 流水线 / Phase 5 真机烟测 SOP           |
+ | 21   | [Win7 LTS 维护](./21-win7-lts.md)               | 18 个月归档时间表 / Win7 用户 Web 化迁移 / 真机烟测 SOP / 风险声明                            |
```

- [ ] **Step 2: 改章节编号（顺延 21→22, 22→23, ..., 25→26）**

```diff
- | 21   | [LLM 代理路由](./21-llm-proxy.md)                  | `/api/v1/llm/*` 透传上游,绕开浏览器到 Ollama/OpenAI 的 CORS 拦截                               |
+ | 22   | [LLM 代理路由](./22-llm-proxy.md)                  | `/api/v1/llm/*` 透传上游,绕开浏览器到 Ollama/OpenAI 的 CORS 拦截                               |
- | 22   | [Agents CRUD 端到端](./22-agents-crud.md)          | list/update/toggle 三层链路 (后端路由 → Electron IPC → 前端 API → UI)                          |
+ | 23   | [Agents CRUD 端到端](./23-agents-crud.md)          | list/update/toggle 三层链路 (后端路由 → Electron IPC → 前端 API → UI)                          |
- | 23   | [Chat 流式响应端到端](./23-chat-streaming.md)      | NDJSON 协议 + Electron IPC event 桥接 + NDJSON relay + chatStream 中间态文案                   |
+ | 24   | [Chat 流式响应端到端](./24-chat-streaming.md)      | NDJSON 协议 + Electron IPC event 桥接 + NDJSON relay + chatStream 中间态文案                   |
- | 24   | [Skills 系统端到端](./24-skills-system.md)         | InprocSkillAdapter + 3 路由 + 3 Electron IPC 命令 + 4 builtin skills 端到端可见 (list/toggle/execute) |
+ | 25   | [Skills 系统端到端](./25-skills-system.md)         | InprocSkillAdapter + 3 路由 + 3 Electron IPC 命令 + 4 builtin skills 端到端可见 (list/toggle/execute) |
- | 25   | [LLM Wiki 集成 (PR-8)](./25-llm-wiki-integration.md) | 4 LLM provider 抽象 + prompt 模板 + LanceDB RAG + 知识图谱 8 阶段实施 |
+ | 26   | [LLM Wiki 集成 (PR-8)](./26-llm-wiki-integration.md) | 4 LLM provider 抽象 + prompt 模板 + LanceDB RAG + 知识图谱 8 阶段实施 |
```

- [ ] **Step 3: 验证**

```bash
grep -E "^\| (20|21|22|23|24|25|26)" docs/technical/README.md
```

Expected: 看到 7 行（20-26）

---

## Task 6: 更新 docs/11-structure.md

**Files:**
- Modify: `docs/11-structure.md`

- [ ] **Step 1: 找桌面壳段落**

```bash
grep -n "Tauri\|tauri\|桌面" docs/11-structure.md | head -10
```

- [ ] **Step 2: 替换为 Electron**

找到段落，把：

```markdown
- **桌面壳**：Tauri 2.x
```

替换为：

```markdown
- **桌面壳**：Electron 21.4.4
```

- [ ] **Step 3: 加新章节"E. Electron 桌面壳"**

在合适位置（src 目录树之后）加：

```markdown
### E. Electron 桌面壳

- `electron/main.ts` — Electron 主进程（FastAPI 子进程 + BrowserWindow + IPC）
- `electron/preload.ts` — contextBridge 注入 `window.electronAPI`
- `electron-builder.yml` — 跨平台打包配置
- `tsconfig.electron.json` — Electron TypeScript 配置
- `tests/electron/` — Playwright-Electron 烟测
```

---

## Task 7: 跑全量验证

**Files:** 无（仅验证）

- [ ] **Step 1: 跑 prettier**

```bash
npx prettier --check docs/technical/20-electron.md docs/technical/21-win7-lts.md docs/technical/README.md docs/11-structure.md
```

Expected: `All matched files use Prettier code style!`

如果有错：

```bash
npx prettier --write docs/technical/20-electron.md docs/technical/21-win7-lts.md docs/technical/README.md docs/11-structure.md
```

---

## Task 8: 提交 + 开 PR

**Files:** 暂存所有变更

- [ ] **Step 1: 检查状态**

```bash
git status --short
```

Expected: 看到
- `R` docs/technical/20-electron-win7.md → 20-electron.md
- `A` docs/technical/21-win7-lts.md
- `D` docs/technical/20-win7-tauri-compat.md (如果有)
- `A` archive/tech-docs-2026-06-13/20-win7-tauri-compat.md (如果有)
- `M` docs/technical/README.md
- `M` docs/11-structure.md

- [ ] **Step 2: 暂存**

```bash
git add -A
```

- [ ] **Step 3: 提交**

```bash
git commit -m "docs(technical): generalize 20-electron-win7.md → 20-electron.md, add 21-win7-lts.md

- 20-electron.md：去掉 Win7 限定，作为通用 Electron 桌面壳文档
- 21-win7-lts.md：新增 Win7 LTS 独立章节（18 个月归档时间表 + Web 化迁移）
- 20-win7-tauri-compat.md：归档（如存在）到 archive/tech-docs-2026-06-13/
- docs/technical/README.md：章节编号顺延（21→22, 22→23, ..., 25→26）
- docs/11-structure.md：加 Electron 桌面壳章节（E）

Refs: docs/superpowers/specs/2026-06-13-electron-branch-strategy-design.md"
```

- [ ] **Step 4: 推 + 开 PR**

```bash
git push -u origin docs/electron-generalization
gh pr create --title "docs(technical): generalize Electron doc + add Win7 LTS chapter" --body "见 commit message"
```

Expected: PR URL 输出（target: main）

---

## Self-Review Checklist (执行前)

- [ ] Task 1-8 全部步骤可执行（无占位符）
- [ ] win7 BRANCH_NOTES.md 完整替换 Tauri 1.6 字样
- [ ] main 20-electron.md 重命名 + 内容更新
- [ ] main 21-win7-lts.md 新建（5 阶段时间表完整）
- [ ] docs/technical/README.md 章节编号顺延无遗漏
- [ ] 跨分支 PR 顺序：先 win7 PR（Task 1），后 main PR（Task 2-8）
- [ ] 18 个月时间表与 spec 第 7 节一致
