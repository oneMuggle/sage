# UI Mock: <标题>

> 复制本文件为 `docs/mocks/<topic>.md` 并填写，同名 HTML 原型放
> `docs/mocks/<topic>.html`。简单的单屏改动可以只写 HTML（把设计意图写进
> HTML 头注释），涉及多状态 / 多流程的提案必须配这份说明。

| 项 | 内容 |
|---|---|
| 状态 | 📝 草案 / 🔍 评审中 / ✅ 已通过 / 🚀 已实现 / 🗑️ 已废弃 |
| Mock 文件 | [`<topic>.html`](./<topic>.html) |
| 关联计划 / PR | `docs/plans/…` 或 PR #… |
| 作者 | @… |
| 创建日期 | YYYY-MM-DD |
| 评审人 | @… |

## 1. 设计目标

- **要解决的问题（现状痛点）**：
- **目标用户与场景**：
- **成功标准**（可观察、可验证）：
  - [ ] 标准 1
  - [ ] 标准 2

## 2. 交互流程

描述关键路径与状态机。复杂流程用 ASCII 图：

```
就绪 → 点击触发 → 进行中 → 完成态
```

| # | 状态 | 触发条件 | UI 表现 | 边界情况 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |

## 3. 技术约束

- **设计令牌**：只用 `tailwind.config.js` / `src/index.css` 的共享令牌
  （速查表见 [README.md](./README.md)），不临时引入新色值
- **组件复用**：优先复用 `src/components/` 现有组件；需新增的 primitive 列在这里
- **性能预算**：首屏 / 动画帧率 / 包体积要求
- **可访问性**：键盘可达、aria-live 播报、尊重 `prefers-reduced-motion`
- **兼容性**：Electron Chromium 106（main 分支）；Win7 LTS 分支同步时注意
  Python 3.8 / 旧 Chromium 约束

## 4. 本次不做（YAGNI）

- 明确列出不在本次范围内的点，防止评审时 scope 膨胀：
- …

## 5. 评审结论

| 日期 | 评审人 | 结论 | 后续动作 |
|---|---|---|---|
| | | | |

> 评审通过后在此粘贴关键评审意见；实现落地后把状态改为 🚀 已实现，
> 并注明对应 PR / 组件路径。
