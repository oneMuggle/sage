# T5：双扫描器规则统一（main AST 版补齐 win7 regex 版差异规则）

日期：2026-09-24
分支：feat/py38-scan-t5（main）/ win7 侧后续单独 PR
状态：T5a 实施完成，待 PR

## 背景

仓库现存两个 py38 兼容扫描器，规则集已漂移：

- main：`backend/tools/py38_hazard_scan.py`（AST，CI collect job；#1500/#1514）
  ——有 py39 stdlib 规则（removeprefix/zoneinfo/graphlib/functools.cache/
  dataclass slots），缺 `Path.is_relative_to`、`datetime.UTC`。
- win7：`scripts/check_py38_compat.py`（regex，CI backend-py38 job）
  ——有 is_relative_to/datetime.UTC，缺 py39 stdlib 规则。

## 内容（T5a，main 侧）

1. 扫描器新增：
   - R11：`Path.is_relative_to`（3.9+，限定接收者为 Name/Attribute/常量）
   - R12：`datetime.UTC`（3.11+，用 timezone.utc）
2. 首轮扫描命中 2 处 `is_relative_to`，均为 **except AttributeError 守卫 +
   relative_to 降级**的认可写法（skills/script_runner.py、skills/validation.py），
   加 `# py38-ok` 行豁免；`datetime.UTC` 零命中。

## 后续（T5b，win7 侧）

- 把 `backend/tools/py38_hazard_scan.py` 同步进 win7，`backend-py38` job
  增加一步扫描（保留既有 check_py38_compat.py 双保险），逐步统一规则集。

## 验证

- py38 解释器全库扫描：0 地雷
- ruff check 改动文件：All checks passed
- 两处命中文件 py38 下 import 正常（豁免为纯注释，零行为变更）
