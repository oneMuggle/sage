local.properties
.gradle/
build/
*.iml
.idea/
captures/
*.apk
*.keystore
# 根 .gitignore 的 `data/` 规则会把本模块的源码目录 ai/arena/companion/data/（core 与 app 各一份）
# 连同 androidTest 下的 data/room 一并忽略：这里显式放回，否则 `git add android` 会静默漏掉源码。
!**/data/
# 真机烟测的临时产物（截图 / uiautomator dump）留在本地，不入库。
screen_*.png
window_dump.xml
main_dump_txt.xml
